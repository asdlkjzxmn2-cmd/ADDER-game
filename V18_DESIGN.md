# V18 주간 트렌드

## 날짜와 주기
기존 state.day와 dateFor를 그대로 사용합니다. 시작 날짜는 2027.01.15, 영업마감 때 기존 코드가 day를 1 늘립니다. 내부 구간은 floor((day-1)/7)로 계산하며 별도 주차 상태나 타이머는 저장하지 않습니다. 2027.01.15~01.21 한 구간, 01.22부터 다음 구간입니다. 월은 기존 30일 게임 달력 그대로입니다. 월/시즌 경계가 구간 중간에 있어도 7일간 트렌드는 유지됩니다.

## 스타일과 변화
기존 8개 스타일(레이어링·광택·유틸리티·시어·볼륨·미니멀·고프코어·페미닌)을 사용합니다. 기존 worldSeed로 정한 순서를 순환하여 인접 구간의 주력 트렌드가 반드시 바뀌고 8구간 후 반복됩니다. 매 구간 상승 2개, 하락 1개이며 서로 겹치지 않습니다. 재실행/세이브 불러오기로 같은 날짜의 트렌드가 바뀌지 않습니다. 현실 날짜나 네트워크는 필요 없습니다.

상품의 기존 trend를 첫 태그로 사용하고 이름의 Cargo/Utility→유틸리티, Wide/Volume→볼륨, Minimal/Basic→미니멀 등 명확한 단어로 태그를 보완합니다(최대 3개). 컬러별 재고가 있는 구조이므로 상품 전체를 Red처럼 단일 컬러로 분류하지 않았습니다. 새 필드는 productStyleTags[상품ID]이며 기존 분류 데이터는 변경하지 않습니다. 기존/시즌 생성/새 거래처/자체제작 상품에 모두 적용됩니다.

## 판매 계산 연결
V17 productSalesFactors의 trend 항목만 다음 식으로 교체합니다.

trend = clamp(1 + (상승 보정 - 하락 보정) × 상품 trendAffinity, 0.85, 1.35)

- 주력 상승 태그 일치: +0.22
- 보조 상승 태그 일치: +0.12
- 두 상승 태그 모두 일치: 큰 값 +0.22만 사용
- 하락 태그 일치: -0.10
- 상승·하락 동시 일치: 서로 합산(예: 0.22-0.10)
- 아무 태그도 일치하지 않음: 1.00

이 배율을 온라인·오프라인 판매에 한 번 적용합니다. V17 기본 인기도/수요/속도/가격 민감도/촬영 배율과 기존 품질·시즌·리뷰·매장·이벤트는 유지됩니다. 예전 주력 스타일 일치 +55% 배율과 중복 적용하지 않습니다. 유행보다 가격·촬영·재고 선택이 중요하도록 효과를 완화했습니다. 난수와 재고 상한 때문에 당일 판매량은 보장되지 않습니다. 기존 이벤트는 별도 배율이며 새 이벤트 시스템은 만들지 않았습니다.

## UI
HOME의 기존 NOW 카드 안에 상승/하락 배지 3개만 추가했습니다. HUD 기존 유행 표시는 주력 상승 스타일을 표시합니다. 새 DAY/WEEK 카운터는 없고 기존 날짜 표시는 그대로입니다. 기존 게임에 있던 DAY 표기도 새로 추가한 것이 아닙니다. 가격 버튼/구매·리오더/촬영·재고 조작은 변경하지 않았습니다.

## 세이브
IndexedDB adder-local-v1 버전 1 / games / current-game과 JSON ADDER_BOUTIQUE_SAVE schema 1 유지. 이전 세이브에 태그가 없으면 해당 상품 정의로 안전하게 생성하며, 정상 태그는 유지합니다. 잘못된 태그는 제거하고 유효값이 없으면 기본 태그로 복원합니다. 현금·날짜·가격·재고·촬영·거래 이력·V17 성향은 그대로 보존합니다. 이전 수동 슬롯도 불러올 때 보완합니다. 추세는 기존 day/worldSeed에서 계산하므로 새로 저장할 날짜 필드가 없습니다.

## 수동 업로드(PC 검수 및 별도 승인 후)
현재 저장소 https://github.com/asdlkjzxmn2-cmd/ADDER-game 에 이 adder-game 폴더 내부를 올리세요. docs 전체와 source, ADDER_V18.html, release/build, 문서를 함께 교체/추가하고 versions는 추가만 합니다. 이전 루트 ADDER_V16.html/ADDER_V17.html을 임의 삭제하지 마세요. 기존 main /docs, 기존 Pages 주소를 그대로 사용합니다. .gitattributes는 docs 파일의 줄바꿈 변환을 막아 캐시 무결성 검증에 필요한 바이트를 보존합니다.

## 변경 파일
기능: source/logic.js. 표시: source/client.js, source/style.css. 버전: template.html, rebuild.cjs, release.json. 생성물: ADDER_V18.html, docs/index.html, docs/service-worker.js. 원본 저장 런타임·SW 템플릿·manifest·아이콘은 유지했습니다.
